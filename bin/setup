#!/bin/bash -eu

case "$1" in
  -v|--version)
    version="$2"
esac

mkdir -p ${OPENSHIFT_JREBEL_DIR}/{log,lib}
cp -r ${OPENSHIFT_JREBEL_DIR}versions/${version}/libjrebel64.so ${OPENSHIFT_JREBEL_DIR}lib
mkdir ${OPENSHIFT_HOMEDIR}.jrebel
